<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.talabaty">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BaseActivity"></activity>
        <activity android:name=".DeliveryServicesActivity"></activity>
        <activity android:name=".FreeDeliveryActivity"></activity>
        <activity android:name=".HelpDetailsActivity" />
        <activity android:name=".CarServiceActivity" />
        <activity android:name=".RemoteServiceActivity" />
        <activity android:name=".FamilyDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>